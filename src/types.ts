export interface SendToInboxParameters{
    token?: string,
    content?: string,
    index?: number,
    note?: string, 
    checked?: string, 
    checkbox?: string, 
    heading?: string,
    color?: string
}